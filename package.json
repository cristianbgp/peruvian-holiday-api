{
  "name": "peruvian-holiday-api",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts",
    "deploy": "wrangler deploy --minify"
  },
  "dependencies": {
    "hono": "^4.7.11"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}